#include <stdio.h>
#include <math.h>

 /*int main() {
 	
 	printf("\a");
 	
 	int a= 7.5;
 	
 	printf("ingresa un número\n");
 	scanf("%d",&a);
 	printf("%d\a",&a);*/
 	
 	/*int main() {
 	int a,b,res,r;
 	
 	float c,d;
 	
 	c = 8;
 	d = 3;
 	a=a;
 	b=6;
 	res = 0;
 	r = 0;
 	
 	r = c/d;
 	res = a%b;
 	printf("el resultado de la división es %f y su residuo es %d",r,res);*/
 	
 	/*int main() {
 	
    	int a,b, sumaValores,restaValores;
    	float c,d, multiplicacionValores,divisionValores;
    	a = 5;
    	b = 6;
    	c = 4.6;
    	d = 9.2;
    	
    printf("el resultado de la suma es %d\n",a+b);
    printf("el resultado de la reta es %d\n",a-b);
    printf("el resultado de la division es %f\n",d/c);
    printf("el resultado de la multiplicacion es %f\n",c*d);
    
    int main() {

	int a,b, potenciaValores, moduloValores;
    	a = 5;
    	b = 6;
		
		printf("el resultado de la potencia de valores es %f\n",(a,b));
		printf("el modulo de valores es %f\n",5%6);
    	 
    int main() {
	
	 printf("expresar el valor logico 1: %d\n ",(5 > 3 && 10 <= 10));
	 printf("expresar el valor logico 2: %d\n ",(50 > 75 || 10 <= 10));
	 printf("expresar el valor logico 3: %d\n ",!(5 >= 3));
	 printf("expresar el valor logico 4: %d\n ",!(5 <= 3));
	
	
	int main() {
		
		if ( !( ! ( 5 > 3 ) && ( 10 <= 10 ) ) )
		printf("instruccion dentro de if\n");
		printf("instruccion fuera de if\n");
		
		int main() 
		{
		
		if (  5 > 3 &&  10 <= 10  )
		{
		
		printf("instruccion dentro de if\n");
		}
		else
		{
		
		printf("instruccion dentro de else\n");
		}
		
		printf("instruccion fuera de if-else\n");
		
	int main() 
	{	
		{
		
		if (  5 > 3 &&  10 <= 10  )
	
		printf("instruccion dentro de if\n"); 
		
		if ( 5 ==3)
	
	
	{ printf("instruccion anidada dentro de if\n"); }
		else
		

	{ printf("instruccion anidada dentro de else\n"); }	
}
		printf("instruccion dentro de if-else\n"); */
		
		
int main() 
{

	int a = 100;
	if (a==10)
		
		{ 
		  printf("el valor es 10\n"); 
		}
		else 
		 if (a==20)
		
		{ 
		  printf("el valor es 20\n"); 
		}
		else
		
		{ 
		  printf("no es 10 o 20\n"); 
		}
	  
	  	
 	return 0; 
 }
